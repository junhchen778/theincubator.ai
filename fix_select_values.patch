--- a/client/src/pages/firm/company-detail.tsx
+++ b/client/src/pages/firm/company-detail.tsx
@@ -720,7 +720,7 @@ export default function FirmCompanyDetailPage() {
                 <Select value={assignedTo} onValueChange={setAssignedTo}>
                   <SelectTrigger>
                     <SelectValue placeholder="Unassigned" />
                   </SelectTrigger>
                   <SelectContent>
-                    <SelectItem value="">Unassigned</SelectItem>
+                    <SelectItem value="unassigned">Unassigned</SelectItem>
                     {teamMembers.map((member) => (
                       <SelectItem key={member.user_id} value={member.user_id}>
                         {member.user?.full_name} {member.title && `(${member.title})`}
@@ -935,7 +935,7 @@ export default function FirmCompanyDetailPage() {
               <SelectTrigger>
                 <SelectValue placeholder="Change assignment" />
               </SelectTrigger>
               <SelectContent>
-                <SelectItem value="">Unassigned</SelectItem>
+                <SelectItem value="unassigned">Unassigned</SelectItem>
                 {teamMembers.map((member) => (
                   <SelectItem key={member.user_id} value={member.user_id}>
                     {member.user?.full_name}
